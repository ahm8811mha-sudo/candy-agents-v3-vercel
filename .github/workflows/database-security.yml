name: Database Security

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "supabase/migrations/**"
      - "supabase/tests/**"
      - ".github/workflows/database-security.yml"
  push:
    branches: [main]
    paths:
      - "supabase/migrations/**"
      - "supabase/tests/**"
      - ".github/workflows/database-security.yml"

permissions:
  contents: read

jobs:
  rls-regression:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PGURL: ${{ secrets.SUPABASE_STAGING_DB_URL }}

    steps:
      - uses: actions/checkout@v4

      - name: Detect staging database configuration
        id: configuration
        shell: bash
        run: |
          if [ -n "$PGURL" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "SUPABASE_STAGING_DB_URL is not configured; database evidence remains incomplete."
          fi

      - name: Install PostgreSQL client
        if: steps.configuration.outputs.configured == 'true'
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Verify ordered migrations parse on staging
        if: steps.configuration.outputs.configured == 'true'
        shell: bash
        run: |
          set -euo pipefail
          for migration in $(find supabase/migrations -maxdepth 1 -type f -name '*.sql' | sort); do
            echo "Applying $migration"
            psql "$PGURL" -v ON_ERROR_STOP=1 -f "$migration"
          done

      - name: Run all database regression suites
        if: steps.configuration.outputs.configured == 'true'
        shell: bash
        run: |
          set -euo pipefail
          for suite in $(find supabase/tests -maxdepth 1 -type f -name '*.sql' | sort); do
            echo "Running $suite"
            psql "$PGURL" -v ON_ERROR_STOP=1 -f "$suite"
          done

      - name: Store database evidence
        if: steps.configuration.outputs.configured == 'true'
        shell: bash
        run: |
          psql "$PGURL" -Atc "select now(), current_database(), current_user" > database-security-evidence.txt
          echo "commit=${GITHUB_SHA}" >> database-security-evidence.txt
          echo "migration_count=$(find supabase/migrations -maxdepth 1 -type f -name '*.sql' | wc -l)" >> database-security-evidence.txt
          echo "regression_suite_count=$(find supabase/tests -maxdepth 1 -type f -name '*.sql' | wc -l)" >> database-security-evidence.txt

      - uses: actions/upload-artifact@v4
        if: steps.configuration.outputs.configured == 'true'
        with:
          name: database-security-evidence
          path: database-security-evidence.txt
          retention-days: 30
